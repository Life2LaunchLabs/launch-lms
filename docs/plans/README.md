# Execution plans

Use Jira workpads for small changes. Complex or multi-stage work keeps a checked-in
plan under `active/` with objective, acceptance criteria, progress, decisions,
verification, and recovery notes. Move it to `completed/` with final evidence
instead of deleting history. Durable debt belongs in `docs/quality/`.
